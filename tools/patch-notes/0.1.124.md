**Bonesaw 0.1.124 - patch notes**

**Changes**
- Auto-Attune part 2: the module sources for the revived path (server-side TryAutoAttune + AASET|lvl + AA sync field, client-side Min ilvl button and popup) - completes 3bdc28659
- Auto-Attune actually works now: the retired auto-attune path is revived behind real filters - the master switch (migrated OFF so nobody's loot starts disappearing), the per-quality opt-outs, and a new minimum item level (AASET|lvl and a Min ilvl button on the items tab); duplicates and already-attuned entries are never consumed (report #228)
- Playerbots react to item links posted in chat and the insult pools grow - ChatReplyAction::HandleItemLinkReply answers any |Hitem message from a new reply_item_link pool in the channel it was posted in instead of the generic not-understand lines, and 75 new toxic/taunt chatter lines land (record 0060, report #229 tail)
- Avenger's Shield: clear the client's 30s CATEGORY cooldown entry (1158) when re-arming the 6s override - the server was already at 6s but the cast packet left a category entry that kept the icon locked; fixes the recurring 30s reports (81/112/187/207/230)
- /bug now opens the same report form as /report (report #223) - the stock Blizzard bug frame files into a table nobody reads on this realm, so overriding it loses nothing; bare /bug opens the form, /bug <text> files a plain bug like /bugreport

**Please test these things**

Still waiting on a report from you:
- back to getting "missing reagent: x" when trying to craft from reagent bank (recurring bug)
- playerbots still opening trade with us randomly, need to disable that entirely (recurring)
- [Insect Swarm] only applying to all enemies in an area, not automatically spreading to nearby enemies (including refreshing duration on existing targets)
- [recurring] loot is saying it will bind when picked up or equipped again
- [Feature #122] bump affliction dot damage up to 2000%
- [Haunt] doesn't seem to be applying [Unstable Affliction] (nor did selecting affliction teach me that spell)
Close Wow and run Bonesaw.exe to update.
